local elixir = require("elixir")

elixir.setup {
  nextls = {
    enable = true, -- defaults to false
  },
  credo = {
    enable = false, -- defaults to true
  },
  elixirls = {
    enable = false,
  }
}
